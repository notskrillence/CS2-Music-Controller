[project]
name = "cs2-music-controller"
version = "0.2.5"
description = "Native Windows music and custom event-sound controller for Counter-Strike 2."
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
